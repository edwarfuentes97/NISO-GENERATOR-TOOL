# Carpeta de salida (no se sube a GitHub)

Aquí aparecen los PNG y PDF que generas con `npm run …`. Git ignora esos archivos para que el repositorio en GitHub pese poco.

Cada comando crea una subcarpeta (por ejemplo `salida/posts-nuevos/`).
